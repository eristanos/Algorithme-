#include <stdio.h>
#include <stdlib.h>

int main(){
    int n = rand() % 10000 + 1;
    int guess = -1;
    while (guess != n)
    {
        printf("donnez un nombre : ")        /* code */
    }
    
    
}